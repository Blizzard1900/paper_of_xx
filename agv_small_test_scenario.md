# AGV 小规模测试场景

## 1. 场景目标

该测试场景用于 Day 1 阶段验证以下核心能力：

1. `data_loader` 能否正确读取节点、边、任务和 AGV 参数；
2. `scheduler_decoder` 能否将人工给定调度方案解码为实际执行路径；
3. `fitness_function` 能否正确计算时间、能耗、充电次数和惩罚值；
4. 充电插单逻辑是否能够在低电量条件下正确触发。

该测试场景强调**可人工验算**，因此任务数量和 AGV 数量均控制在较小规模。

---

## 2. 测试参数

### 2.1 全局参数

采用与主模型一致的参数：

- AGV 速度：
  \[
  v_0=0.5\ \mathrm{m/s}
  \]
- 单位距离电量消耗：
  \[
  \beta=5\times 10^{-4}
  \]
- 拥堵敏感系数：
  \[
  \alpha=0.6
  \]
- 工人干扰率：
  \[
  \lambda_{\mathrm{prod}}=0.02
  \]
- 确定性计算阶段采用期望工人干扰停时：
  \[
  \mathbb{E}[\xi]=\frac{1}{\lambda_{\mathrm{prod}}}=50\ \mathrm{s}
  \]
- 换道惩罚：
  \[
  t^{\mathrm{lc}}=2\ \mathrm{s}
  \]

### 2.2 AGV 参数

设两台 AGV：

| AGV | 起始节点 | 初始电量 | 最大电量 | 充电阈值 | 最小安全电量 | 充电功率 | 载重上限 |
|---|---|---:|---:|---:|---:|---:|---:|
| AGV_1 | C1 | 6.0 | 10.0 | 4.0 | 2.0 | 10.0 | 100 |
| AGV_2 | J5 | 3.2 | 10.0 | 4.0 | 2.0 | 10.0 | 100 |

说明：

- `AGV_1` 初始电量较高，用于验证普通调度流程；
- `AGV_2` 初始电量低于阈值，用于验证充电插单逻辑。

---

## 3. 任务集合

定义 4 个测试任务：

| 任务ID | 任务类型 | 取货点 | 送货点 | 数量 | 最早开始 | 最晚完成 | 取货服务时长 | 卸货服务时长 |
|---|---|---|---|---:|---:|---:|---:|---:|
| T1 | 原料配送 | S_1_1 | L1_1 | 20 | 0 | 2000 | 30 | 30 |
| T2 | 原料配送 | S_2_3 | L2_2 | 15 | 0 | 2200 | 30 | 30 |
| T3 | 成品转运 | L4_2 | F_1_2 | 10 | 200 | 2600 | 20 | 20 |
| T4 | 不良品转运 | L4_4 | D_3 | 8 | 300 | 2600 | 20 | 20 |

其中：

- `S_1_1`、`S_2_3` 属于物料区；
- `L1_1`、`L2_2` 为生产线服务节点；
- `F_1_2` 为成品区节点；
- `D_3` 为不良品区节点。

---

## 4. 人工调度方案

为验证解码器，指定一组人工调度方案：

- `AGV_1` 执行任务顺序：`[T1, T3]`
- `AGV_2` 执行任务顺序：`[T2, T4]`

### 4.1 人工调度解释

#### AGV_1

1. 从 `C1` 出发；
2. 前往 `S_1_1` 执行任务 `T1` 取货；
3. 从 `S_1_1` 前往 `L1_1` 执行任务 `T1` 卸货；
4. 前往 `L4_2` 执行任务 `T3` 取货；
5. 从 `L4_2` 前往 `F_1_2` 执行任务 `T3` 卸货。

#### AGV_2

1. 从 `J5` 出发；
2. 由于初始电量低于阈值，优先插入一次充电任务；
3. 从最近空闲充电位返回后执行任务 `T2`；
4. 执行任务 `T4`。

---

## 5. 解码检查项

对于上述人工调度方案，解码器需要输出以下结果。

### 5.1 路径层结果

对每台 AGV 输出：

1. 访问节点序列；
2. 使用的边序列；
3. 每段边的开始时刻与结束时刻；
4. 是否经过充电位；
5. 是否发生等待。

### 5.2 任务层结果

对每个任务输出：

1. AGV 编号；
2. 取货开始时间；
3. 取货完成时间；
4. 卸货开始时间；
5. 卸货完成时间；
6. 是否违反时间窗。

### 5.3 电量层结果

对每台 AGV 输出：

1. 初始电量；
2. 每完成一段路径后的剩余电量；
3. 充电开始时刻；
4. 充电结束时刻；
5. 最终剩余电量。

---

## 6. 评价指标输出格式

对该调度方案，适应度函数至少需要输出如下指标：

| 指标 | 符号 | 说明 |
|---|---|---|
| 最大完工时间 | \(T_{\max}\) | 两台 AGV 中最后完成任务的时刻 |
| 总里程 | \(D_{\mathrm{total}}\) | 所有 AGV 运行总距离 |
| 总能耗 | \(E_{\mathrm{total}}\) | 所有 AGV 总电量消耗 |
| 时间窗违约数 | \(N_{\mathrm{late}}\) | 超过最晚完成时间的任务个数 |
| 充电次数 | \(N_{\mathrm{charge}}\) | 调度过程中触发的充电次数 |
| 拥堵等待时间 | \(W_{\mathrm{cong}}\) | 因边占用冲突产生的累计等待 |
| 目标函数值 | \(F\) | 综合目标值或适应度值 |

---

## 7. 预期验证逻辑

### 7.1 路径正确性验证

- `AGV_1` 应能正常完成两项任务且不触发充电；
- `AGV_2` 应在执行 `T2` 前触发充电；
- 所有路径均应由图中合法边组成。

### 7.2 电量正确性验证

若 `AGV_2` 未触发充电即直接执行全部任务，则应出现：

\[
q_k(t)<q^{\mathrm{th}}
\]

因此调度解码器必须自动插入充电任务。

### 7.3 时间窗正确性验证

若服务完成时刻满足

\[
C_i\le l_i
\]

则记为按时完成，否则记为时间窗违约。

### 7.4 目标函数验证

若采用综合适应度：

\[
F=\omega_1T_{\max}+\omega_2E_{\mathrm{total}}+\omega_3W_{\mathrm{cong}}+\omega_4N_{\mathrm{late}}+\omega_5P_{\mathrm{battery}}+\omega_6P_{\mathrm{capacity}}
\]

则该小规模场景可用于逐项核对每个组成项是否被正确累加。

---

## 8. Day 1 核心验收标准

若程序能够对上述测试场景输出以下内容，则视为 Day 1 基础框架通过：

1. 每台 AGV 的完整节点路径；
2. 每个任务的开始/结束时间；
3. 每台 AGV 的电量变化轨迹；
4. 自动插入的充电任务；
5. 最大完工时间、总里程、总能耗、违约数、充电次数和目标函数值。

通过该小规模场景验证后，即可进入 Day 2 的遗传算法编码与求解阶段。
